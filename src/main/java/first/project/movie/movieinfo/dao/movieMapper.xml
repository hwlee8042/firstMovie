<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	
<mapper namespace="first.project.movie.movieinfo.dao.MovieDAO">
	
	<insert id = "insertMovie" parameterType="first.project.movie.movieinfo.model.MovieVO">
		insert into
			movie(
				movie_number,
				movie_name,
				movie_genre,
				movie_reldate,
				movie_exit,
				movie_showtime,
				movie_director,
				movie_actor,
				movie_story,			
				movie_rating,
				movie_running,			
				movie_site,
				movie_url1,
				movie_url2,
				movie_url3,
				movie_postername,
				movie_posterpath
			)
			values(
				movie_seq.nextval,
				#{movie_name},
				#{movie_genre},
				to_date(#{movie_reldate}, 'yyyy-MM-dd'),
				to_date(#{movie_reldate}, 'yyyy-MM-dd') + #{movie_running},
				#{movie_showtime},
				#{movie_director},
				#{movie_actor},
				#{movie_story},
				#{movie_rating},
				#{movie_running},
				#{movie_site},
				#{movie_url1},
				#{movie_url2},
				#{movie_url3},
				#{poster_name},
				#{poster_path}
			)
	</insert>
	
	
</mapper>